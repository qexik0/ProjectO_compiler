class Program is
    this(a: Boolean, b: Boolean) is
        Console.println(a.Or(b))
        Console.println(a.And(b))
        Console.println(a.Xor(b))
        Console.println(a.Not)

        Console.println(a.And(b).Not)
        Console.println(a.Or(b).Not)
    end
end