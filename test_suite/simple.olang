class SomeClass is
    var z : Integer(6)
    var y : z
    this() is
        Integer(10).Plus(5).Less(10)
        return
    end
end

class Program is
    var x : SomeClass()
    var y : x
    var z : true
    var a : z
    method met(x: Integer, y: Real, z: Boolean) is
        return
    end
    this(x: Real, y: Real, z: Real) is
        Console(x)
        Console(y)
        Console(z)
        if Integer(10).Plus(5).Less(10) then
            Console(10)
        else
            Console(15)
        end
        var t : Integer(10)
        Console(t)

        Console(false)
        return
    end
    this(x: Integer, y: Real, z: Boolean) is
        return
    end
end